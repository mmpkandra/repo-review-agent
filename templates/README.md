# Templates

This directory is reserved for future externalized documentation templates.
The current implementation keeps templates in Python for simplicity, but the
doc generator already accepts a template directory so file-backed templates can
be introduced without changing the public interface.
